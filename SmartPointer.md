# 1. 스마트 포인터?
### (1) unique_ptr
 하나의 객체만 소유가 가능한, 포인터  
 객체 삭제시(소멸자로), 객체가 소유권 가져야 함. 
 move()로 소유권 이전, 복제는 불가.  
> unique_ptr<자료형> 포인터명1(new 자료형(value)) 로   생성 및 초기화  
>auto 포인터명2 = move(포인터명1)  포인터명2로 소유권 이전  
>> auto란, 초기화 식에서 형식이 추론되는 변수 선언함.(C11, 초기화 값이 있는 경우에만 동작)  
>> 함수 매개변수 사용 x, 구조체|클레스 멤버 변수 사용 x  
>  
> unique_ptr<자료형> 포인터명3 = 포인터명1 -> 오류!!  
> 포인터명2.reset() 가르키는 메모리영역 삭제.(값만!)  
> 가르키는 메모리 해제 뿐, **포인터는 nullptr이 됨.**  
>   
> 해당 포인터가 stack에 저장시 -> scope 벗어나면 삭제됨.
> heap에 저장(new사용) -> delete 필요.


### (1.5) make_unique (C++14 이후)
 전달받은 인수로 지정된 타입의 객체 생성 후,  
 **그 객체를 가르키는 unique_ptr 반환**  
 예외 발생에 대해 안전히 대체 가능.  
 (정의 중 예외 발생시 포인터의 메모리 자기가 삭제함.)  

 > unique__ptr<자료형> 포인터이름 = make_unique<자료형>(값);
 
### (2) shared_ptr
 